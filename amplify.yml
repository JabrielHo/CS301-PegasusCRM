version: 1
frontend:
    phases:
        build:
            commands:
                - 'npm run build'
    artifacts:
        baseDirectory: ./ITSA_Team2/
        files:
            - '**/*'
    cache:
        paths: []
